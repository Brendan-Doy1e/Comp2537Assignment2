<%- include("templates/header") %> 
    <% if (session.authenticated) { %>
<div class="container">
    <h2>Hello, <%= session.name %>!</h2>
    <form action='/members' method='get'><button>Go to Members Area</button></form>
    <form action='/signout' method='get'><button>Sign Out</button></form>
</div>
    <% } else { %>
<div class="container">
    <form action='/create-user' method='get'><button>Sign Up</button><br></form>
    <form action='/login' method='get'><button>Log in</button></form>
</div>
    <% } %>
<%- include("templates/footer") %>